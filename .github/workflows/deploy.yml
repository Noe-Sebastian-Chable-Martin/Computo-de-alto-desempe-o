name: Deploy Iris Plots to GitHub Pages

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 */5 * * *'  # Ejecutar cada 5 minutos (máximo permitido por GitHub Actions)

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install elasticsearch pandas

    - name: Run script to generate data
      env:
        ES_ENDPOINT: ${{ secrets.ES_ENDPOINT }}
        ES_API_KEY: ${{ secrets.ES_API_KEY }}
      run: python create_index.py

    - name: Commit and push changes to docs
      run: |
        git config user.name "GitHub Action"
        git config user.email "action@github.com"
        git add docs/*
        git commit -m "Update data.json in docs folder" || echo "No changes to commit"
        git push